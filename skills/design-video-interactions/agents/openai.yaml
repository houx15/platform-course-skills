interface:
  display_name: "设计视频暂停交互"
  short_description: "检查课程视频格式，安全生成候选文件，并设计结构化暂停互动"
  default_prompt: "使用 $design-video-interactions 检查这个课程视频；需要处理格式或大小时先给出安全方案并等待我确认，保留原件，候选视频经我播放确认后再写入课程，同时设计和验证结构化暂停互动。"
